using System;
using System.Collections.Generic;
using System.Text;

namespace test2
{
    class _37異常捕獲
    {
        /*static void Main(string[] args)
        {
            // 1.作用域 2.初始值，因為number不一定是int
            int number = 0;
            bool b = true;
            Console.WriteLine("請輸入一個數字");
            // try 和 catch中間不能有代碼
            try
            {
                // 可能出現異常的代碼
                number = Convert.ToInt32(Console.ReadLine());
            }
            catch
            {
                // 出現異常後要執行的代碼
                // 異步函數
                Console.WriteLine("error");
                b = false;
            }
            if (b)
            {
                Console.WriteLine(number * 2);
            }
            
            Console.ReadKey();
        }*/
    }
}
